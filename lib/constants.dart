import 'package:flutter/material.dart';

const kPrimaryColor = Color(0xFF6F35A5);
const kPrimaryLightColor = Color(0xFFF1E6FF);

//admin colors
const AdminBG = Color(0xF7F3FA);
const iconUnverified = Color.fromRGBO(0, 0, 0, 0.75);
const nameUnverified = Color.fromRGBO(0, 0, 0, 0.40);
const adminUnverified = Color.fromRGBO(0, 0, 0, 0.50);
const textVerified = Color.fromRGBO(111, 53, 165, 0.50);

const double defaultPadding = 16.0;

//spacing between admin fields
const double spacing = 10.0;

//spacing between admin fields